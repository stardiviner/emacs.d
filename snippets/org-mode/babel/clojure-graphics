# -*- mode: snippet -*-
# name: babel-clojure-graphics
# key: babel-clojure-graphics
# group: babel.clojure
# --
#+begin_src clojure :dir "data/images" :results graphics :file "${1:inline image filename}.png"
(use '(incanter core stats datasets charts io pdf))

(def $1 $0)
#+end_src
