# -*- mode: snippet -*-
# name: babel-install-emacs-package
# key: babel-install-emacs-package
# group: babel.install
# expand-env: ((yas-indent-line 'fixed))
# --
#+begin_src emacs-lisp
(unless (package-installed-p '${1:package})
  (package-install '$1))
#+end_src