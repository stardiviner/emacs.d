# -*- mode: snippet -*-
# name: clj install deps through edn
# key: babel-clj-install-edn
# group: babel.clojure
# expand-env: ((yas-indent-line nil) (yas-wrap-around-region 'nil))
# --
**** Clojure CLI/deps.edn

#+begin_src sh :async
clj -Sdeps '{:deps \{${1:${2:name} \{:mvn/version "${3:version}"\}}\}\}'
#+end_src